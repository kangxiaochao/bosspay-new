<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyfd.dao.mp.PhonePhysicalchannelDao">
    <select id="selectCount" resultType="java.lang.Integer">
        select count(id) from mp_specify_section_recharge
    </select>

    <select id="selectAll" resultType="java.util.Map">
        select * from mp_specify_section_recharge
    </select>

    <insert id="phonePhysicalchanneladd" parameterType="java.util.Map">
          insert into mp_specify_section_recharge (id, dispatcher_provider_id, section,create_time)
    values (#{id,jdbcType=VARCHAR}, #{dispatcher_provider_id,jdbcType=VARCHAR}, #{section,jdbcType=VARCHAR},CURRENT_TIMESTAMP
    </insert>



    <selectKey keyProperty="id" resultType="String" order="BEFORE">select  replace(uuid(),'-','') as id  from dual</selectKey>
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    insert into mp_phone_section (id, section, provider_id,
    province_code, city_code, update_date,
    update_user, create_date, create_user, provider_type, carrier_type
    )
    values (#{id,jdbcType=VARCHAR}, #{section,jdbcType=VARCHAR}, #{providerId,jdbcType=VARCHAR},
    #{provinceCode,jdbcType=VARCHAR}, #{cityCode,jdbcType=VARCHAR},
    #{updateDate,jdbcType=TIMESTAMP}, #{updateUser,jdbcType=VARCHAR}, CURRENT_TIMESTAMP,
    #{createUser,jdbcType=VARCHAR},#{providerType,jdbcType=VARCHAR},#{carrierType,jdbcType=VARCHAR}
    )

</mapper>